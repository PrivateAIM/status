# Deliberately unpinned: the client is versioned against the Hub API, and every
# outage in this repo so far has been the pin lagging behind a cluster upgrade.
# Tracking the latest release inverts the risk - the client can now run ahead of
# the cluster instead of behind it - so a failing run is worth checking against
# the deployed version, readable without credentials at the service root:
#   curl https://core.staging.privateaim.net/
flame-hub-client
